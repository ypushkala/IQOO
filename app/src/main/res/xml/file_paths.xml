<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the temporary share folder is ever exposed, and only for the one file the user chose to share. -->
    <cache-path name="share" path="share/" />
</paths>
